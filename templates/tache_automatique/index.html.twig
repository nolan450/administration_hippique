{% extends 'base.html.twig' %}

{% block title %}TacheAutomatique index{% endblock %}

{% block body %}
    <h1>TacheAutomatique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_tache_auto</th>
                <th>Nom_action</th>
                <th>Frequence_realisation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tache_automatique in tache_automatiques %}
            <tr>
                <td>{{ tache_automatique.id }}</td>
                <td>{{ tache_automatique.idTacheAuto }}</td>
                <td>{{ tache_automatique.nomAction }}</td>
                <td>{{ tache_automatique.frequenceRealisation }}</td>
                <td>
                    <a href="{{ path('app_tache_automatique_show', {'id': tache_automatique.id}) }}">show</a>
                    <a href="{{ path('app_tache_automatique_edit', {'id': tache_automatique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tache_automatique_new') }}">Create new</a>
{% endblock %}
