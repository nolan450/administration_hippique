{% extends 'base.html.twig' %}

{% block title %}TacheAutomatique{% endblock %}

{% block body %}
    <h1>TacheAutomatique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tache_automatique.id }}</td>
            </tr>
            <tr>
                <th>Id_tache_auto</th>
                <td>{{ tache_automatique.idTacheAuto }}</td>
            </tr>
            <tr>
                <th>Nom_action</th>
                <td>{{ tache_automatique.nomAction }}</td>
            </tr>
            <tr>
                <th>Frequence_realisation</th>
                <td>{{ tache_automatique.frequenceRealisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tache_automatique_index') }}">back to list</a>

    <a href="{{ path('app_tache_automatique_edit', {'id': tache_automatique.id}) }}">edit</a>

    {{ include('tache_automatique/_delete_form.html.twig') }}
{% endblock %}
