<form method="post" action="{{ path('app_club_hippique_delete', {'id': club_hippique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ club_hippique.id) }}">
    <button class="btn">Delete</button>
</form>
