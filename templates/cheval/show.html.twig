{% extends 'base.html.twig' %}

{% block title %}Cheval{% endblock %}

{% block body %}
    <h1>Cheval</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cheval.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cheval.nom }}</td>
            </tr>
            <tr>
                <th>Race</th>
                <td>{{ cheval.race }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cheval.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cheval_index') }}">back to list</a>

    <a href="{{ path('app_cheval_edit', {'id': cheval.id}) }}">edit</a>

    {{ include('cheval/_delete_form.html.twig') }}
{% endblock %}
