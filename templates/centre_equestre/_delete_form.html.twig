<form method="post" action="{{ path('app_centre_equestre_delete', {'id': centre_equestre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centre_equestre.id) }}">
    <button class="btn">Delete</button>
</form>
