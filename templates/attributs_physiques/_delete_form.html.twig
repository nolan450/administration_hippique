<form method="post" action="{{ path('app_attributs_physiques_delete', {'id': attributs_physique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attributs_physique.id) }}">
    <button class="btn">Delete</button>
</form>
