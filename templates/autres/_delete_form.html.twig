<form method="post" action="{{ path('app_autres_delete', {'id': autre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autre.id) }}">
    <button class="btn">Delete</button>
</form>
