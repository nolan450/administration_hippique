{% extends 'base.html.twig' %}

{% block title %}AttributsMentaux{% endblock %}

{% block body %}
    <h1>AttributsMentaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attributs_mentaux.id }}</td>
            </tr>
            <tr>
                <th>Sociabilite</th>
                <td>{{ attributs_mentaux.sociabilite }}</td>
            </tr>
            <tr>
                <th>Intelligence</th>
                <td>{{ attributs_mentaux.intelligence }}</td>
            </tr>
            <tr>
                <th>Temperament</th>
                <td>{{ attributs_mentaux.temperament }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attributs_mentaux_index') }}">back to list</a>

    <a href="{{ path('app_attributs_mentaux_edit', {'id': attributs_mentaux.id}) }}">edit</a>

    {{ include('attributs_mentaux/_delete_form.html.twig') }}
{% endblock %}
