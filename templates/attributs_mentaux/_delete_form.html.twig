<form method="post" action="{{ path('app_attributs_mentaux_delete', {'id': attributs_mentaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attributs_mentaux.id) }}">
    <button class="btn">Delete</button>
</form>
